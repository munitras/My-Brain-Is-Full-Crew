# Stream 2

## **Stream 2: Workflow Enhancements**.

### Epic 6: The Foreman (Proactive Task Management)
Turning the vault from a passive storage box into an active daily planner.

| Unit Ref    | Task Description                | Design / Architectural Notes                                                                                                                                                                         | Acceptance Criteria                                                                                                            |
|:------------|:--------------------------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------|
| **WRK-601** | **Create Foreman Agent Script** | Since this is a data-aggregation task, bypass the LLM. Write `scripts/foreman-sweep.sh` using `grep` and `awk` to find all open markdown checkboxes (`- [ ]`) across `01-Projects/` and `00-Inbox/`. | `[x]` Script executable.<br>`[x]` Accurately extracts open tasks with file source links.<br>`[x]` Outputs clean markdown list. |
| **WRK-602** | **Update Dashboard Note**       | Embed the Foreman's output into `Meta/Dashboard.md`. Use Dataview to render tasks tagged with `#urgent` or deadlines matching today's date at the very top.                                          | `[x]` Dataview query written for tasks.<br>`[x]` Urgent tasks highlighted.<br>`[x]` Dashboard serves as a unified HUD.         |
| **WRK-603** | **Inject into ON_START.md**     | Update `.opencode/ON_START.md` to trigger the Foreman sweep during the morning boot sequence, ensuring the task list is fresh when the user opens the vault.                                         | `[ ]` Boot sequence updated.<br>`[ ]` Terminal cleanly advises user to check the HUD.                                          |

### Epic 7: The Reader (Web Ingestion & Synthesis)
Frictionless capture of external research.

| Unit Ref    | Task Description                | Design / Architectural Notes                                                                                                                                             | Acceptance Criteria                                                                                                                    |
|:------------|:--------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:---------------------------------------------------------------------------------------------------------------------------------------|
| **WRK-701** | **Create Reader Subagent File** | Create `.opencode/agents/reader.md`. Set `mode: subagent` and explicitly grant `webfetch: allow`.                                                                        | `[ ]` File created with correct OpenCode frontmatter.<br>`[ ]` Permissions correctly scoped.                                           |
| **WRK-702** | **Define Reader System Prompt** | Instruct the Reader to fetch the URL, extract the core thesis, and format it using our strict YAML rules (with the `summary:` field). All output must go to `00-Inbox/`. | `[ ]` Prompt enforces YAML schema.<br>`[ ]` Output restricted to `00-Inbox/`.<br>`[ ]` Excludes ads/nav boilerplate from fetched text. |
| **WRK-703** | **Update Dispatcher Routing**   | Update `AGENTS.md` to route triggers like "Read this link", "Summarize this article", or raw URLs to the Reader subagent.                                                | `[ ]` Routing rules updated in dispatcher.<br>`[ ]` Handoff to Sorter documented via JSONL bus.                                        |

### Epic 8: The Synthesizer (From Storage to Output)
Turning the graph into actionable documents.

| Unit Ref    | Task Description                     | Design / Architectural Notes                                                                                                                       | Acceptance Criteria                                                                                                |
|:------------|:-------------------------------------|:---------------------------------------------------------------------------------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------|
| **WRK-801** | **Create Synthesizer Subagent File** | Create `.opencode/agents/synthesizer.md`. Set `mode: subagent`. This agent needs massive context window limits.                                    | `[ ]` File created.<br>`[ ]` `mode: subagent` configured.                                                          |
| **WRK-802** | **Define Synthesizer Prompt**        | Instruct it to query the Seeker for related notes (or a specific MOC), read the sources, and draft a structured document (essay, proposal, email). | `[ ]` Prompt requires citations to source notes.<br>`[ ]` Outputs generated to a new `01-Projects/Drafts/` folder. |
| **WRK-803** | **Implement Drafts Folder**          | Update `Meta/vault-structure.json` to include a dedicated location for AI-generated drafts so they don't pollute source knowledge.                 | `[ ]` JSON structure updated.<br>`[ ]` Path validation script recognizes new path.                                 |

### Epic 9: The Evening Shutdown Macro
Enforcing Inbox Zero programmatically.

| Unit Ref    | Task Description                   | Design / Architectural Notes                                                                                                                         | Acceptance Criteria                                                                  |
|:------------|:-----------------------------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------|:-------------------------------------------------------------------------------------|
| **WRK-901** | **Create ON_CLOSE.md**             | Create `.opencode/ON_CLOSE.md`. This macro should trigger the Sorter to process the inbox, then trigger the Librarian for a quick broken-link check. | `[ ]` File created with macro instructions.<br>`[ ]` Triggers Sorter batch mode.     |
| **WRK-902** | **Create Daily Reflection Prompt** | Add a step in `ON_CLOSE.md` where the CLI asks the user: *"Any final thoughts to park for tomorrow?"* and passes the response to the Scribe.         | `[ ]` Interactive prompt functions.<br>`[ ]` Response successfully routed to Scribe. |
| **WRK-903** | **Create Shutdown Alias**          | Provide operators with a bash alias (e.g., `alias brainsleep="opencode --prompt 'Execute ON_CLOSE.md...'"`) to seamlessly trigger the workflow.      | `[ ]` Alias documented and tested by operators.                                      |

---
